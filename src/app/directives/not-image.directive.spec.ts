import { NotImageDirective } from './not-image.directive';

describe('NotImageDirective', () => {
  it('should create an instance', () => {
    const directive = new NotImageDirective();
    expect(directive).toBeTruthy();
  });
});
